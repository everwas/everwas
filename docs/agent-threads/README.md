# Agent threads

Async agent-to-agent coordination messages (see the thread protocol in the
operator's global config). Thread directories are gitignored because they
reference live infrastructure; only decisions get promoted into tracked docs.
