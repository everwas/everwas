# Copy to .env and adjust.
COMPOSE_PROJECT_NAME=everwas-site

# Public hostname the site is served on. Swap for a registered domain
# (e.g. everwas.org) when one exists — nothing else needs to change.
DOMAIN=everwas.supported.systems
