# Copy to .env and adjust.
COMPOSE_PROJECT_NAME=openrmm-site

# Public hostname the site is served on. Swap for a registered domain
# (e.g. openrmm.org) when one exists — nothing else needs to change.
DOMAIN=openrmm.supported.systems
