# Copy to .env and adjust. Never commit .env.
COMPOSE_PROJECT_NAME=openrmm-docs

# Public hostname (caddy-docker-proxy). The placeholder domain until the
# project has a registered one; swap here + public/robots.txt + DNS.
DOMAIN=docs.openrmm.supported.systems
