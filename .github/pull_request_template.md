**What this changes and why**

**Checklist**

- [ ] Commits are signed off (`git commit -s`, see CONTRIBUTING.md)
- [ ] Tests pass (`uv run pytest` / `go test ./...`)
- [ ] If a NATS subject changed: contract doc and both builders moved together
- [ ] License boundary respected (agent/ is Apache-2.0, everything else AGPL-3.0)
